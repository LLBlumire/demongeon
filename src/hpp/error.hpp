/* error.hpp
 * Provides the Error state tracking Enum.
 */
#ifndef INCL_ERROR_HPP
#define INCL_ERROR_HPP

namespace demon {
    /* enum Error
     * Represents possible error states.
    */
    namespace Error {
        enum Enum {
            UnexpectedLoopTermination
        };
    }
}

#endif
