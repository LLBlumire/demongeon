#ifndef INCL_DIRECTION_HPP
#define INCL_DIRECTION_HPP

namespace demon {
    namespace Direction {
        enum Enum {
            North,
            East,
            South,
            West
        };
    }
}

#endif
