/* direction.hpp
 * Provides the Direction enum, representing cardinal directions.
 */

#ifndef INCL_DIRECTION_HPP
#define INCL_DIRECTION_HPP

namespace demon {
    /* enum Direction
     * Represents the four cardinal directions
     */
    namespace Direction {
        enum Enum {
            North,
            East,
            South,
            West
        };
    }
}

#endif
